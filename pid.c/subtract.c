#include <stdio.h>
#include <unistd.h>

int main(void)
{
	int a = 50;
	int b = 36;
	int subtract = a - b;
	pid_t = ppid;

	ppid = getppid();

	printf("diff = %d\n", subtract);
	printf("ppid = %u\n", ppid);

	return 0;
}
